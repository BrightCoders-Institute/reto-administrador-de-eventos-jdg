<nav class="navbar navbar-expand-lg navbar-light shadow-sm p-3 mb-5 rounded">
  <div class="container">
    <%= link_to root_path, class: 'navbar-brand navbar-image' do %>
      <%= image_tag("events_logo.png", alt: "Events logo" , class: "navbar-logo" ) %>
        <% end %>

          <div class="navbar-collapse d-flex align-items-center justify-content-center">
            <% if user_signed_in? %>
              <p class="navbar-text text-center">User: <strong> <strong>
                    <%= current_user.name %>
                  </strong></p>
              <% end %>
          </div>

          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>

          <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
            <%= link_to 'START' , show_event_path, class: 'btn btn-primary me-3' %>
              <% if user_signed_in? %>
                <%= link_to "LOGOUT" , log_out_path, class: 'btn btn-secondary' %>
                  <% else %>
                    <%= link_to "LOGIN" , user_session_path, class: 'btn btn-dark' %>
                      <% end %>
          </div>
  </div>
</nav>
